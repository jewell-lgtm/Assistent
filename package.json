{
  "name": "local-assistent",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "gen": "node scripts/gen-userspace.mjs",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  },
  "dependencies": {
    "@assistant/capabilities-server": "workspace:*"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
